import React from 'react'

const Image = (props) => {
    const user = props.match.params.username
    

    return (
        <div>
            good image
            
            
        </div>
    )
}

export default Image
